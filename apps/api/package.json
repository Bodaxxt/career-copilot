{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "lint": "flake8 . && black --check .",
    "lint:fix": "black .",
    "test": "pytest"
  }
}
